export function FavoritesPage() {
  return (
    <section className="rounded-lg border border-peony/50 bg-mist p-6 shadow-sm">
      <p className="text-sm font-medium uppercase tracking-wide text-bark">Favorites</p>
      <h2 className="mt-3 text-3xl font-bold">Saved orchids coming next</h2>
      <p className="mt-3 max-w-2xl text-base leading-7 text-ink/80">
        This route is ready for local browser favorites once favorite storage is added.
      </p>
    </section>
  );
}
